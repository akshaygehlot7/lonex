import React from 'react'

const Logout = () => {
    // let result = await fetch("https://newuser-mp6z.onrender.com/api/login", {
    //     method: "POST",
    //     body: JSON.stringify(items),
    //     headers: {
    //       "Content-Type": "application/json",
    //       Accept: "application/json",
    //     },
    //   });
    //   result = await result.json();
    //   if (result.success) {
    //     // Save the user token in localStorage with the key "user-token"
    //     localStorage.setItem("user-token", result.data.token);
  
    //     // Save other user information if needed
    //     localStorage.setItem("user-info", JSON.stringify(result));
  
    //     // Navigate to the home page or perform other actions
    //     navigate("/home");
    //   } else {
    //     console.error("Login failed. Server response:", result);
    //     window.alert("Login failed. Server response:", result);
    //   }
    // } catch (error) {
    //   console.error("Error during login:", error);
    //   window.alert("Error during login:", error);
    // }
  return (

    <div>Logout</div>
  )
}

export default Logout